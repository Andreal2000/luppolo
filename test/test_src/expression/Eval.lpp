Main() {
    Result = 0

    if !(Eval(x^2 + 3*x + 2, 2) == 12) { Result = Result + a }

    if !(Eval(x^3 - 2*x^2 + x, -1) == -4) { Result = Result + b }

    if !(Eval(3*x^2 + 2*y - z, 2, 1, 4) == 10) { Result = Result + c }

    if !(Eval(w * x + y - z^2, 3, 4, 5, 6) == -19) { Result = Result + d }
    
    if !(Eval(x/y + z, 1/2, 1/3, 1/4) == 7/4) { Result = Result + e }

    return Result
}