Main() {
    Result = 0

    if !(x * (y^2 * z) == x * z * y^2) { Result = Result + a }

    if !(3*x^2 * 2*y == 6 * y * x^2) { Result = Result + b }

    if !(x^(-1) * (1+y^2) * x^4 * (1 + y^2) == (1 + y^2)^2 * x^3) { Result = Result + c }

    return Result
}